<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Update Product</title>

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css"/>

  <style>
    body {
      background: linear-gradient(to right, #f3eadf,  #f3ede4);
    }

    .form-container {
      min-height: 250px;
      margin-top: 80px;
      background-color: #ffffff;
      border-radius: 12px;
      padding: 30px;
    }
  </style>
</head>
<body>
  <%- include("nav.ejs") %>

  <div class="container form-container w-50">
    <h2 class="text-center text-primary">Update Product</h2>

    <form action="/saveproduct" method="post">
      <input type="hidden" name="pid" value="<%= prod[0].pid %>" class="form-control">

      <div class="form-group">
        <label class="font-weight-bold h5">Product Name</label>
        <input type="text" name="pname" value="<%= prod[0].pname %>" class="form-control" required>
      </div>

      <div class="form-group">
        <label class="font-weight-bold h5">Select Category</label>
        <select name="cid" class="form-control" required>
          <% updatecate.forEach((item, index) => { %>
            <option value="<%= item.cid %>"><%= item.name %></option>
          <% }) %>
        </select>
      </div>

      <div class="form-group">
        <input type="submit" value="Update Product" class="btn btn-primary btn-block">
      </div>
    </form>
  </div>

</body>
</html>
