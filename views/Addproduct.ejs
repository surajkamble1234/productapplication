<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Product</title>

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css"/>

  <style>
    body {
      background: linear-gradient(to right, #f3eadf, #f3ede4);
    }

    .form-container {
      min-height: 250px;
      margin-top: 80px;
      background-color: #ffffff;
      border-radius: 12px;
      padding: 30px;
    }

    .message {
      display: block;
      margin-top: 15px;
      font-weight: 500;
      color: green;
    }
  </style>
</head>
<body>
  <%- include("nav.ejs") %>

  <div class="container form-container w-50">
    <h2 class="text-center text-primary">Add Product</h2>

    <form action="/saveprod" method="post">
      <div class="form-group">
        <label class="font-weight-bold h5">Product Name</label>
        <input type="text" name="pname" placeholder="Enter Product" class="form-control" required />
      </div>

      <div class="form-group">
        <label class="font-weight-bold h5">Select Category</label>
        <select name="cid" class="form-control" required>
          <% getca.forEach((item, index) => { %>
            <option value="<%= item.cid %>"><%= item.name %></option>
          <% }) %>
        </select>
      </div>

      <div class="form-group">
        <input type="submit" value="Add Product" class="btn btn-block bg-primary text-white" />
      </div>

      <% if (msg) { %>
        <label class="text-center message"><%= msg %></label>
      <% } %>
    </form>
  </div>
</body>
</html>
