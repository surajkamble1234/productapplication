<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Category</title>

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css"/>

  <style>
    body {
      background: linear-gradient(to right, #f3eadf,  #f3ede4);
    }

    .form-container {
      min-height: 250px;
      margin-top: 80px;
      background-color: #ffffff;
      border-radius: 12px;
      padding: 30px;
    }

    .message {
      display: block;
      margin-top: 15px;
      font-weight: 500;
      color: green;
    }
  </style>
</head>
<body>
  <%- include("nav.ejs") %>

  <div class="container  form-container w-50">
    <h2 class="text-center text-primary ">Add Category</h2>

     <form name="frm" action="/savecate" method="POST">
     <div class="form-group">
     <label class=" font-weight-bold h5">
      Category Name
     </label>
     <input type="text" name="name" placeholder="Enter Category" class="form-control" required />
    </div>


      <div class="form-group">
        <input type="submit" value="Add Category" class="btn btn-submit btn-block bg-primary" />
      </div>

      <% if (msg) { %>
        <label class="text-center message"><%= msg %></label>
      <% } %>
      
    </form>
  </div>

</body>
</html>
